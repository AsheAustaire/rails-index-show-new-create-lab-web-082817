<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <% @coupons.each do |coupon| %>
    <%= coupon.store %>
    <%= link_to(coupon.coupon_code, coupon_path(coupon.id))  %>
    <% end %>
  </body>
</html>
